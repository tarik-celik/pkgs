# pkgs
simple package manager

## installation
$ git clone https://github.com/tarik-celik/pkgs

$ cd pkgs

$ sh install.sh

## usage

### install:

$ ./pkgs -i (package name)

### remove:

$ ./pkgs -r (package name)

#### or:
$ rm -rf (package name)
